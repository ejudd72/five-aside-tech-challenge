import React, { Component } from 'react';
import { FormControl, InputGroup, Button, Form, Alert } from "react-bootstrap";

const Alert = ({ children }) => (   
    dismissable> 
            <p>{ message }</p>
            <Button onClick={ handleAcceptOddTeam }>Yes</Button></Alert>
)
    

export default Alert;